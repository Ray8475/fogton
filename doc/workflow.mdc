# Соглашение по процессу работы (Workflow)

Правила выполнения работ по [tasklist.md](tasklist.md) для code-ассистента. Разработка по [vision.md](../vision.md). KISS.

---

## Порядок работы

1. **Строго по плану** — выполняем только текущую итерацию из [tasklist.md](tasklist.md); не перескакивать и не объединять итерации без явного согласования.
2. **Перед реализацией** — предложить решение: перечислить шаги и **показать ключевые фрагменты кода** (файлы, функции, сигнатуры). Дождаться согласования от пользователя.
3. **После согласования** — реализовать изменения в коде.
4. **После реализации** — дождаться подтверждения пользователя (проверка по пункту «Тест» в тасклисте).
5. **Обновить прогресс** в [tasklist.md](tasklist.md):
   - в таблице «Отчёт по прогрессу» поставить статус текущей итерации (🔄 В работе → ✅ Готово), указать дату и примечание при необходимости;
   - отметить выполненные пункты чекбоксами: `- [ ]` → `- [x]`.
6. **Переход к следующей итерации** — только после подтверждения пользователя и обновления тасклиста.
7. **Коммит** — после завершения итерации сделать коммит в репозиторий (осмысленное сообщение, например: «Iteration N: краткое описание»).

---

## Краткая схема

```
План (tasklist) → Согласовать решение (с фрагментами кода) → Реализовать →
→ Подтверждение пользователя → Обновить tasklist (статус, чекбоксы) →
→ Коммит → Согласовать переход к следующей итерации → Повторить
```

---

## Важно

- Не начинать следующую итерацию без явного согласования перехода.
- Не менять объём итерации без согласования (не добавлять лишнее из следующих шагов).
- Ориентироваться на [vision.md](../vision.md) и [conventions.md](../conventions.md) при предложении решения.
