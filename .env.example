# Конфигурация: только env (см. vision.md — Конфигурирование).
# Скопировать в .env и заполнить; .env не коммитить.

### Telegram (бот и Mini App)
BOT_TOKEN=put-telegram-bot-token-here
# Публичный HTTPS для webhook (при своём домене — см. doc/setup-with-your-domain.md)
WEBHOOK_BASE_URL=https://your-public-https-domain.example
# Токен Cloudflare Tunnel для run-bot-retry.ps1 (опционально; иначе туннель запускать вручную)
# CLOUDFLARED_TUNNEL_TOKEN=eyJhIjoi...
WEBHOOK_SECRET=change-me
# Если Telegram временно не резолвит домен — бот будет повторять setWebhook() с этим интервалом
WEBHOOK_SET_RETRY_SECONDS=180
WEBAPP_URL=https://your-miniapp-url.example
BOT_MODE=polling

### Backend (API)
JWT_SECRET=change-me-too
JWT_REFRESH_SECRET=change-me-refresh-too
# Время жизни access токена в секундах (300 = 5 минут)
JWT_TTL_SECONDS=300
# Время жизни refresh токена в секундах (604800 = 7 дней)
JWT_REFRESH_TTL_SECONDS=604800
DATABASE_URL=sqlite:///./app.db
ADMIN_TOKEN=change-me-admin

### TON (депозиты, webhook)
TON_WEBHOOK_SECRET=change-me-ton
TON_PROJECT_WALLET_ADDRESS=EQ...
TON_API_PROVIDER_NAME=toncenter_or_other
TON_API_KEY=put-key-here